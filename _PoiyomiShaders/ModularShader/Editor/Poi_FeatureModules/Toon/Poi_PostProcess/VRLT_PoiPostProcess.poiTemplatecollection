#T#PoiPostProcessProperties
//ifex _PostProcess==0
[HideInInspector] m_start_postprocess (" Post Processing--{reference_property:_PostProcess,button_help:{text:Tutorial,action:{type:URL,data:https://www.poiyomi.com/post-processing/main},hover:Documentation}}", Float) = 0
[HideInInspector][ThryToggle(POSTPROCESS)]_PostProcess ("Enable", Float) = 0
[TextureKeyword][sRGBWarning][ThryTexture] _PPMask ("Mask--{reference_properties:[_PPMaskPan, _PPMaskUV, _PPMaskChannel, _PPMaskInvert]}", 2D) = "white" { }
[ThryHideInInspector][Vector2]_PPMaskPan ("Panning", Vector) = (0, 0, 0, 0)
[ThryHideInInspector][Enum(R, 0, G, 1, B, 2, A, 3)]_PPMaskChannel ("Channel", Float) = 0
[ThryHideInInspector][ToggleUI]_PPMaskInvert ("Invert", Float) = 0
[ThryHideInInspector][ThryWideEnum(UV0, 0, UV1, 1, UV2, 2, UV3, 3, Panosphere, 4, World Pos, 5, Local Pos, 8, Polar UV, 6, Distorted UV, 7, Matcap, 9, Screen Space, 10)]_PPMaskUV ("UV", Int) = 0

[HideInInspector] s_start_PPHueShift ("Hue Shift--{persistent_expand:true,default_expand:true}", Float) = 1
[ThryWideEnum(OKLab, 0, HSV, 1)] _PPHueShiftColorSpace ("Hue Color Space", Int) = 0
[ThryWideEnum(Hue Select, 0, Hue Shift, 1)] _ppHueSelectOrShift ("Select or Shift", Int) = 1
_PPHue ("Hue", Range(0, 1)) = 0
[Enum(R, 0, G, 1, B, 2, A, 3)]_PPHueOffsetMask ("Hue Offset Mask", Int) = 0
_PPHueOffset ("Hue Offset", Range(0, 1)) = 0
_PPHueSpeed ("Hue Speed", Float) = 0
[HideInInspector] s_end_PPHueShift ("", Float) = 0

[HideInInspector] s_start_PPColorAdjust ("Color Adjust--{persistent_expand:true,default_expand:true}", Float) = 1
[HDR]_PPTint ("Tint", Color) = (1, 1, 1, 1)
[VectorLabel(R,G,B)]_PPRGB ("RGB", Vector) = (1, 1, 1, 1)
_PPContrast ("Contrast", Float) = 1
_PPSaturation ("Saturation", Float) = 1
_PPBrightness ("Brightness", Float) = 1
_PPLightness ("Lightness", Float) = 0
_PPHDR ("HDR Boost", Float) = 0
[HideInInspector] s_end_PPColorAdjust ("", Float) = 0

[HideInInspector] s_start_PPPosterization ("Posterization--{reference_property:_PPPosterization,persistent_expand:true,default_expand:true}", Float) = 0
[HideInInspector][ThryToggleUI(true)] _PPPosterization (" Posterization", Float) = 0

_PPPosterizationAmount ("Steps", Range(1, 30)) = 4

[HideInInspector] s_end_PPPosterization ("Posterization", Float) = 0

[HideInInspector] m_end_postprocess ("", Float) = 0
//endex

#T#PoiPostProcessKeywords
//ifex _PostProcess==0
#pragma shader_feature_local POSTPROCESS
#pragma shader_feature_local PROP_PPMASK
//endex

#T#PoiPostProcessVariablesExposed
//ifex _PostProcess==0
#ifdef POSTPROCESS
	float4 _PPMask_ST;
	float2 _PPMaskPan;
	float _PPMaskUV;
	float _PPMaskChannel;
	half _PPMaskInvert;

	half3 _PPTint;
	float3 _PPRGB;
	float _PPHue;
	float _PPHueSpeed;
	float _PPHueOffset;
	float _PPHueOffsetMask;
	float _PPContrast;
	half _PPSaturation;
	half _PPBrightness;
	float _PPLightness;
	float _PPHDR;
	half _PPHueShiftColorSpace;
	float _ppHueSelectOrShift;
	float _PPPosterization;
	float _PPPosterizationAmount;
#endif
//endex

#T#PoiPostProcessVariables
//ifex _PostProcess==0
#ifdef POSTPROCESS
	#if defined(PROP_PPMASK)
		Texture2D _PPMask;
	#endif
	const static float COLORS = 32;

#endif
//endex


#T#PoiPostProcessFunctions
//ifex _PostProcess==0
#ifdef POSTPROCESS
	float3 poiPosterize(float3 color, float steps)
	{
		half3 newColor = RGBtoHSV(color);
        steps = floor(steps);
		newColor.r = floor(newColor.r * steps) / steps;
		newColor.g = floor(newColor.g * steps) / steps;
		newColor.b = floor(newColor.b * steps) / steps;
		return HSVtoRGB(newColor);
	}

	float oetf_sRGB_scalar(float L)
	{
		float V = 1.055 * (pow(L, 1.0 / 2.4)) - 0.055;
		if (L <= 0.0031308)
			V = L * 12.92;
		return V;
	}

	float3 oetf_sRGB(float3 L)
	{
		return float3(oetf_sRGB_scalar(L.r), oetf_sRGB_scalar(L.g), oetf_sRGB_scalar(L.b));
	}

	float eotf_sRGB_scalar(float V)
	{
		float L = pow((V + 0.055) / 1.055, 2.4);
		if (V <= oetf_sRGB_scalar(0.0031308))
			L = V / 12.92;
		return L;
	}

	float3 GetHDR(float3 rgb)
	{
		return float3(eotf_sRGB_scalar(rgb.r), eotf_sRGB_scalar(rgb.g), eotf_sRGB_scalar(rgb.b));
	}

	float3 GetContrast(float3 col, float contrast)
	{
		return lerp(float3(0.5, 0.5, 0.5), col, contrast);
	}

	float3 GetSaturation(float3 col, float interpolator)
	{
		return lerp(dot(col, float3(0.3, 0.59, 0.11)), col, interpolator);
	}

	void applyPostProcessing(inout PoiFragData poiFragData, in PoiMesh poiMesh)
	{
		half4 ppMask = 1;
		#if defined(PROP_PPMASK)
			ppMask = POI2D_SAMPLER_PAN(_PPMask, _MainTex, poiUV(poiMesh.uv[_PPMaskUV], _PPMask_ST), _PPMaskPan);
			ppMask = lerp(ppMask, 1 - ppMask, _PPMaskInvert);
		#endif

		float3 col = poiFragData.finalColor;
		col = hueShift(col, _PPHue + _PPHueOffset * ppMask[_PPHueOffsetMask] + frac(POI_TIME.x * _PPHueSpeed), _PPHueShiftColorSpace, _ppHueSelectOrShift);
		col *= _PPTint;
		col *= _PPRGB;
		col = GetSaturation(col, _PPSaturation);
		col = lerp(col, GetHDR(col), _PPHDR);
		col = GetContrast(col, _PPContrast);
		col *= _PPBrightness;
		col += _PPLightness;

		col = lerp(poiFragData.finalColor, col, ppMask[_PPMaskChannel]);

		if (_PPPosterization >= 0.5)
		{
			col = lerp(col, poiPosterize(col, _PPPosterizationAmount), ppMask[_PPMaskChannel]);
		}

		poiFragData.finalColor = col;
	}
#endif
//endex

#T#PoiPostProcessFunctionCalls
//ifex _PostProcess==0
#ifdef POSTPROCESS
	applyPostProcessing(poiFragData, poiMesh);
#endif
//endex
